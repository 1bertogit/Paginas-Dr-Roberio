---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ContactForm from '../components/ContactForm.astro';
---
<BaseLayout
  title="Contato - Landing Rápida"
  description="Entre em contato conosco. Estamos prontos para ajudar."
>
  <Header siteName="Landing" />

  <main>
    <!-- Hero Section -->
    <section class="px-[5vw] py-16 max-w-5xl mx-auto text-center">
      <p class="text-accent uppercase tracking-[0.12em] text-sm mb-4">Contato</p>
      <h1 class="text-4xl sm:text-5xl font-bold leading-tight mb-6">
        Fale conosco
      </h1>
      <p class="text-lg text-slate-300 max-w-2xl mx-auto">
        Tem alguma dúvida ou quer saber mais? Preencha o formulário abaixo ou use um dos nossos canais de contato.
      </p>
    </section>

    <!-- Contact Info + Form -->
    <section class="px-[5vw] py-8 max-w-5xl mx-auto">
      <div class="grid gap-12 lg:grid-cols-3">
        <!-- Contact Info -->
        <div class="lg:col-span-1 space-y-8">
          <div>
            <h3 class="font-semibold text-lg mb-3">E-mail</h3>
            <a
              href="mailto:contato@exemplo.com"
              class="text-slate-400 hover:text-accent transition"
            >
              contato@exemplo.com
            </a>
          </div>

          <div>
            <h3 class="font-semibold text-lg mb-3">Telefone</h3>
            <a
              href="tel:+5511999999999"
              class="text-slate-400 hover:text-accent transition"
            >
              +55 (11) 99999-9999
            </a>
          </div>

          <div>
            <h3 class="font-semibold text-lg mb-3">Endereço</h3>
            <p class="text-slate-400">
              São Paulo, SP<br />
              Brasil
            </p>
          </div>

          <div>
            <h3 class="font-semibold text-lg mb-3">Horário</h3>
            <p class="text-slate-400">
              Segunda a Sexta<br />
              9h às 18h
            </p>
          </div>
        </div>

        <!-- Contact Form -->
        <div class="lg:col-span-2">
          <ContactForm
            title="Envie sua mensagem"
            subtitle="Responderemos o mais breve possível."
          />
        </div>
      </div>
    </section>

    <!-- Map Placeholder -->
    <section class="px-[5vw] py-12 max-w-5xl mx-auto">
      <div class="w-full h-64 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center">
        <p class="text-slate-500">
          Mapa interativo - Adicione sua integração do Google Maps aqui
        </p>
      </div>
    </section>
  </main>

  <Footer siteName="Landing" />
</BaseLayout>
